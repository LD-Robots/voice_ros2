# Core ROS2 dependencies
# (installed via apt: ros-humble-desktop)

# Audio Processing
sounddevice==0.4.6
pyaudio==0.2.14
soundfile==0.13.1
webrtcvad==2.0.10
numpy>=1.24.0

# ASR (Automatic Speech Recognition)
faster-whisper==1.2.1

# LLM (Language Model)
groq==1.0.0

# TTS (Text-to-Speech)
piper-tts==1.3.0

# Wake Word Detection
openwakeword==0.6.0

# Environment Variables
python-dotenv==1.2.1

# Speaker Identification
speechbrain>=1.0.0
torch>=2.0.0
torchaudio>=2.0.0
# torchcodec removed (unused and problematic on ARM)

# Utilities
requests>=2.31.0
