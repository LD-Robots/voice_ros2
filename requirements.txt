# Core ROS2 dependencies are installed separately, for example via
# `sudo apt install ros-humble-desktop` or a newer ROS2 distribution.

# Audio capture, playback, and DSP
numpy>=1.24.0
scipy>=1.10.0
sounddevice==0.4.6
pyaudio==0.2.14
soundfile==0.13.1
webrtcvad==2.0.10

# Speech recognition and text processing
faster-whisper==1.2.1
rapidfuzz>=3.0.0

# LLM and realtime backends
groq==1.0.0
python-dotenv==1.2.1
requests>=2.31.0
websocket-client>=1.8.0

# Text to speech
edge-tts>=6.1.0
piper-tts==1.3.0
num2words>=0.5.13

# Wake word and stop keyword detection
openwakeword==0.6.0
onnxruntime>=1.17.0

# Speaker identification
speechbrain>=1.0.0
huggingface_hub<1.0
torch>=2.0.0
torchaudio>=2.0.0
torchcodec>=0.10.0
